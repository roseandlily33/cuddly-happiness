<section>
   <section>
    <h1 id="dashTitle">Dashboard:</h1>
    <div id="dashboard">
      {{#each post as |postId|}}
      {{> dashPart}}
      {{/each}}
    </div>
    <button id="newPostBtn">Add a Post</button>
</section>
</section>