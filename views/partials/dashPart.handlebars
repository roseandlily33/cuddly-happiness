<section>
    <div class="dashDiv">
        <div>
             <a href="/edit/{{id}}">{{post_title}}</a>
            <h4>Posted by: {{user.username}} on {{post.createdAt}}</h4>
        </div>
    </div>
</section>